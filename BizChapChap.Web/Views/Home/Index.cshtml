<!DOCTYPE html>
<html  ng-app="bizChapChap">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Free Ads, Sell anywhere</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Styles.Render("~/Content/css")
</head>
<body ng-cloak ng-controller="rootCtrl">
    <!-- Fixed navbar -->
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">BizChapChap</a>
            </div>
            <div id="navbar" class="navbar-collapse collapse">
                <ul id="pright" class="nav navbar-nav pull-right">
                    <li><a href="#login">Login</a></li>
                    <li><a href="#register">Signup</a></li>
                    <li>
                        @*<input id="" type="submit" class="btn btn-default button_post_add" value="POST FREE AD" />*@
                        <a id="button_post_add" href="#listingadd">
                            POST FREE AD
                        </a>
                    </li>
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </nav>

    <div ng-show="false">Logged in as {{loggedUser.username}}</div>
    <div class="container" ng-view>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/angularjs")
    @Scripts.Render("~/bundles/angularcss")
    @Scripts.Render("~/bundles/spa")
    @Scripts.Render("~/bundles/owlcarousel")

</body>
</html>
